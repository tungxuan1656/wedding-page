{
  "id": "feat-003",
  "name": "Guest Personalization",
  "description": "URL-based guest personalization (?g=<id>), showing personalized greeting, message, and optional photo. Graceful fallback for unknown guests",
  "dependencies": ["feat-001"],
  "status": "done",
  "evidence": "Landing page now supports `?g=<guestId>` personalization with typed static guest lookup, personalized greeting/message, highlighted guest name in invitation text, optional photo rendering, and graceful fallback for unknown guests. Verified with pnpm lint, pnpm typecheck, pnpm build, and ./init.sh on 2026-05-05.",
  "plan": "docs/exec-plans/plans/feat-003-guest-personalization.md",
  "acceptanceCriteria": [
    "URL pattern: ?g=<guestId> loads personalized content",
    "Guest data defined in lib/guests.ts as typed constant",
    "Personalized greeting shows guest name (e.g., 'Xin chào Anh Tuấn')",
    "Personal message displayed for known guests",
    "Optional guest photo displayed",
    "Unknown guest IDs show generic content (no error state)",
    "Guest name highlighted in invitation text"
  ],
  "notes": "Guest data is static JSON in lib/guests.ts. No database. Vietnamese names and messages."
}
